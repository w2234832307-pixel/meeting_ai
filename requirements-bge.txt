# ==============================================
# BGE-M3 本地向量化依赖
# 使用方法：pip install -r requirements-bge.txt
# ==============================================

# 先安装基础依赖
-r requirements.txt

# BGE-M3 向量化模型
FlagEmbedding>=1.3.0
